[package]
name = "glow_backend"
version = "0.1.0"
edition = "2024"

[dependencies]
angui = { path = "../angui" }
bmp = "0.5.0"
glow = "0.17.0"

[dev-dependencies]
glfw = { version = "0.62.0", default-features = false, features = ["x11", "raw-window-handle-v0-6"] }
